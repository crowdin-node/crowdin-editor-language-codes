{
  "name": "crowdin-editor-language-codes",
  "description": "A mapping of unique language codes used in Crowdin's web editor URLs",
  "version": "1.0.0",
  "main": "codes.json",
  "license": "MIT",
  "devDependencies": {
    "csv-parser": "^1.12.1",
    "jest": "^22.4.3",
    "lodash": "^4.17.5",
    "standard": "^11.0.1",
    "standard-markdown": "^4.0.2"
  },
  "scripts": {
    "test": "jest && standard --fix && standard-markdown",
    "watch": "jest --watch --notify --notifyMode=change --coverage",
    "build": "node script/build.js"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
